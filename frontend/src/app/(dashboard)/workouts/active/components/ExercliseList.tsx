import { Routine } from "@/src/features/routines/types";
import React from "react";

export default function ExercliseList({
  routine,
}: {
  routine: Routine | null;
}) {
  return <div>ExercliseList</div>;
}
